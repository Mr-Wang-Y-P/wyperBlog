{
    "version": 2,
    "builds": [
      {
        "src": "server/server.js",
        "use": "@vercel/node"
      }
    ],
    "routes": [{
      "src": "/(.*)",
      "dest": "/server/server.js"
    }],
    "rewrites": [{ "source": "/:path*", "destination": "/index.html" }]
  }